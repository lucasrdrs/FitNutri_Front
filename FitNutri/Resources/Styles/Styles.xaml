<?xml version="1.0" encoding="UTF-8" ?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <!-- Nova Paleta de Cores -->
    <Color x:Key="Primary">#272B3B</Color>
    <Color x:Key="Secondary">#101422</Color>
    <Color x:Key="Accent">#A8DCE7</Color>
    <Color x:Key="Background">#FFFFFF</Color>
    <Color x:Key="Surface">#D1E0E3</Color>
    <Color x:Key="White">#FFFFFF</Color>
    <Color x:Key="Gray">#757575</Color>

    <!-- Estilos de Botão -->
    <Style TargetType="Button">
        <Setter Property="TextColor" Value="{StaticResource White}" />
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="CornerRadius" Value="10" />
        <Setter Property="HeightRequest" Value="50" />
    </Style>

    <!-- Estilos de Label -->
    <Style x:Key="HeaderLabel" TargetType="Label">
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource Secondary}" />
    </Style>

    <Style x:Key="SubHeaderLabel" TargetType="Label">
        <Setter Property="FontSize" Value="18" />
        <Setter Property="FontAttributes" Value="Bold" />
        <Setter Property="TextColor" Value="{StaticResource Primary}" />
    </Style>

</ResourceDictionary>
